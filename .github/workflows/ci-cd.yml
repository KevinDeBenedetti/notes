name: CI/CD

on:
  push:
    branches: [main]
    paths:
      - 'docs/**'
      - '.github/workflows/**'

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: false

jobs:
  ci:
    name: CI
    uses: KevinDeBenedetti/github-workflows/.github/workflows/ci-shell.yml@main
    with:
      run-shellcheck: false
      run-actionlint: true
      actionlint-paths: .github/workflows/
      run-bats: false
      run-link-check: true
      link-check-paths: 'docs/**/*.md'

  bot-check:
    if: github.event_name == 'pull_request'
    uses: KevinDeBenedetti/github-workflows/.github/workflows/check-bot-commits.yml@main
    permissions:
      contents: read
      pull-requests: read

  # ── Dispatch docs rebuild ──────────────────────────────────────────────────
  dispatch:
    name: Dispatch docs rebuild
    needs: ci
    if: contains(toJSON(github.event.commits.*.modified), 'docs/') || contains(toJSON(github.event.commits.*.added), 'docs/')
    uses: KevinDeBenedetti/github-workflows/.github/workflows/cd-docs.yml@main
    secrets: inherit
